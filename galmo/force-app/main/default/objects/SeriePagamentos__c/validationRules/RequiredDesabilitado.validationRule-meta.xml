<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>RequiredDesabilitado</fullName>
    <active>true</active>
    <description>Impede edição do campo ValorTotal quando Restante está marcado.</description>
    <errorConditionFormula>AND(Restante__c, ISCHANGED(ValorTotal__c))</errorConditionFormula>
    <errorMessage>Impede edição do campo ValorTotal quando Restante está marcado.</errorMessage>
</ValidationRule>
